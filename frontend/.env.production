# Production API endpoint for Vite builds (vite build loads .env.production
# automatically). Public value — it's the browser-visible API base URL.
# Local dev overrides: put VITE_API_URL=http://localhost:8000 in .env.local
VITE_API_URL=https://instaiq-backend.onrender.com
