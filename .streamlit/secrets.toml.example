# Streamlit Cloud secrets template — copy real values into the
# Streamlit Cloud dashboard (App settings -> Secrets), NEVER commit them.
# BACKEND_URL = "https://your-backend.onrender.com"
BACKEND_URL = "http://127.0.0.1:8000"
