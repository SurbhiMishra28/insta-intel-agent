# Serverless entrypoint deps. Vercel installs api/requirements.txt for the
# function. uvicorn is omitted (not needed serverless); everything else is
# already compatible with the Python 3.12 runtime.
fastapi==0.115.0
pydantic==2.9.2
python-dotenv==1.0.1
httpx==0.27.2
sqlalchemy==2.0.35
xhtml2pdf==0.2.16
openai>=1.59,<2
langchain-core>=0.3,<0.4
langchain-openai>=0.2,<0.3
